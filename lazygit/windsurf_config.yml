# Used by: https://github.com/tom-pollak/lazygit-vscode
os:
  edit:              "windsurf --reuse-window -- {{filename}}"
  editAtLine:        "windsurf --reuse-window --goto -- {{filename}}:{{line}}"
  editAtLineAndWait: "windsurf --reuse-window --goto --wait -- {{filename}}:{{line}}"
  openDirInEditor:   "windsurf -- {{dir}}"
promptToReturnFromSubprocess: false
git:
  pagers:
    - pager: delta $(defaults read -g AppleInterfaceStyle >/dev/null 2>&1 && echo --dark || echo --light) --paging=never
